import React from "react";

const AvatarStep = ({ onClickHandler }) => {
  return (
    <>
      <h1>AvatarStep</h1>
      <button onClick={onClickHandler}>Next</button>
    </>
  );
};

export default AvatarStep;
